# wireguard-vps
A lightweight Bash script that automates the creation of a WireGuard VPN server on Ubuntu. The script generates secure server and client key pairs, configures the WireGuard server, creates a client configuration file, and generates a QR code for quick setup on mobile devices.
